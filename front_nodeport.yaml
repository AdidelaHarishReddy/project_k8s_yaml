apiVersion: v1
kind: Service
metadata:
    name: front_nodeport
    labels:
        service: front_NodePort
spec:
   type: NodePort
   selector:
        app: frontend
   ports:
      - name: frontendports
        port: 3000
        targetPort: 3000
        nodePort: 30001
