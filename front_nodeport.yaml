apiVersion: v1
kind: Service
metadata:
    labels:
        service: NodePort
spec:
   type: NodePort
   selector:
        app: frontend
   ports:
      - name: frontendports
        port: 3000
        targetPort: 3000
        nodePort: 30001
